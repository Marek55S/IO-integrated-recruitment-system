# Public GraphQL endpoint used by client-side code
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000

# Server/API endpoint used by tools (codegen) that expect full GraphQL URL
GRAPHQL_API_ENDPOINT=http://localhost:4000/graphql
